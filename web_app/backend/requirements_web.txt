fastapi
uvicorn[standard]
celery[redis]
redis
flower
python-multipart
# Explicit dependencies to satisfy checks
requests>=2.32.0
setuptools>=70.0.0
urllib3>=2.2.0
opencv-python-headless
pillow
transformers<4.46  # Pin to version compatible with PyTorch 2.2
# Pinned to match pytorch/pytorch:2.2.0 base image exactly
torch==2.2.0
torchvision==0.17.0 
timm
scikit-learn
tqdm>=4.66.0
numpy==1.23.5  # Sweet spot for legacy YOLO + modern transformers
mediapipe==0.10.9
protobuf<4.0.0
ultralytics==8.2.10  # Pinned to avoid auto-update bugs
yolov5       # For legacy Burns model loading
